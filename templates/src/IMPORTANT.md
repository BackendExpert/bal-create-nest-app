# IMPORTANT Readme

- After finish Creating project,

- - run

```bash

npm run start:dev

```

- go to your MongoDB compass

- and select `permissions` schema

- import data from `src/dbs/defualt-permissions.json`

- and select `roles` schema

- import data from `src/dbs/defualt-roles.json`

- becuse the Profile and Notifiaction endpoint must need Permssions to Access

# Admin Access

- `SUPER_ADMIN` bypass the Permissions

